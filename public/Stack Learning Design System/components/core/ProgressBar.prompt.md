Thin learning-progress bar with an animated fill; terracota by default, or a technology color on track pages.

```jsx
<ProgressBar value={62} />
<ProgressBar value={40} color="var(--tech-react)" height={4} />
```

`value` is 0–100; fill width animates over 300ms.
